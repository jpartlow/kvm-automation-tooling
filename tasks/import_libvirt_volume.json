{
  "description": "Imports a local image file as a libvirt volume in the default pool.",
  "input_method": "stdin",
  "parameters": {
    "image_path": {
      "type": "String",
      "description": "Absolute path to the image file to be imported."
    },
    "volume_name": {
      "type": "String",
      "description": "The volume name to attach to the image in libvirt. It will be created in the default pool."
    }
  }
}
