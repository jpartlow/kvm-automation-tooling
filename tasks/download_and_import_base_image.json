{
  "description": "Downloads an image from the given url to a given directory, then `virsh vol-download` the image into the default pool.",
  "input_method": "stdin",
  "parameters": {
    "image_url": {
      "type": "String",
      "description": "The URL of the image to download."
    },
    "download_dir": {
      "type": "String",
      "description": "The directory to download the image to."
    },
    "volume_name": {
      "type": "String",
      "description": "The volume name to import the image as in libvirt. It will be created in the default pool."
    }
  }
}
